# TradingView API 통합 가이드

## 목차
1. [TradingView API 개요](#tradingview-api-개요)
2. [API 종류 비교](#api-종류-비교)
3. [아키텍처 설계](#아키텍처-설계)
4. [API KEY 관리 전략](#api-key-관리-전략)
5. [구현 로드맵](#구현-로드맵)
6. [보안 고려사항](#보안-고려사항)

---

## TradingView API 개요

### 📊 TradingView란?
TradingView는 **리얼타임 금융 데이터**와 **고급 차트 기능**을 제공하는 플랫폼입니다.

**주요 특징:**
- 전 세계 주식, 암호화폐, 상품 데이터
- 다양한 기술적 지표 (EMA, RSI, MACD 등)
- 고급 차트 라이브러리 (Lightweight Charts)
- API 레이트 제한: 다양한 플랜별로 상이

---

## API 종류 비교

### 1️⃣ **TradingView Lightweight Charts (무료)**

```
✅ 장점:
  - 완전 무료
  - 가볍고 빠름
  - 반응형 지원
  - 기본 차트 기능

❌ 단점:
  - 리얼타임 데이터 불포함 (수동으로 데이터 제공)
  - 기본 지표만 지원
  - API 제한 없음 (자체 데이터 필요)

📌 용도:
  데이터를 직접 제공하는 경우 최적
```

### 2️⃣ **TradingView Widget (무료/유료)**

```
✅ 장점:
  - 공식 TradingView 차트
  - 리얼타임 데이터
  - 풍부한 기능

❌ 단점:
  - 커스터마이징 제한
  - 브랜딩 표시 (무료판)
  - 유료 기능 필수

📌 용도:
  TradingView 공식 차트 사용 시
```

### 3️⃣ **TradingView Data API (유료)**

```
✅ 장점:
  - RESTful API 제공
  - 리얼타임 및 히스토리 데이터
  - 높은 정확도
  - 프로그래밍 가능

❌ 단점:
  - 비용 발생 ($50~$500+/월)
  - API KEY 필요
  - 레이트 제한 있음

📌 용도:
  완전 제어 및 통합 필요 시
```

### 4️⃣ **Alpha Vantage API (대체안)**

```
✅ 장점:
  - 무료 플랜 제공 (5 calls/min)
  - RESTful API
  - 간단한 통합

❌ 단점:
  - 레이트 제한 낮음
  - 제한된 데이터
  - 리얼타임 X

📌 용도:
  저비용 데이터 소스
```

---

## 아키텍처 설계

### 🏗️ 권장 아키텍처: TradingView Lightweight Charts + 백엔드 데이터 공급

```
┌─────────────────────────────────────────────────────────┐
│                  프론트엔드 (React 19)                     │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  ChartTab.tsx (TradingView Lightweight Charts)       │ │
│  │  - 차트 렌더링                                        │ │
│  │  - 사용자 인터랙션 처리                              │ │
│  │  - 지표 표시                                         │ │
│  └─────────────────────────────────────────────────────┘ │
│                        ↓                                   │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  TradingViewChartService.ts (API 호출)              │ │
│  │  - 데이터 요청 관리                                  │ │
│  │  - 캐싱 로직                                         │ │
│  │  - 에러 처리                                         │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
                        ↓ (HTTP 요청)
┌─────────────────────────────────────────────────────────┐
│              백엔드 (Python/Node.js)                      │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  /api/chart/data                                    │ │
│  │  - TradingView Data API 호출                        │ │
│  │  - API KEY 관리                                     │ │
│  │  - 데이터 가공 및 캐싱                              │ │
│  │  - 속도 제한 (Rate Limiting)                       │ │
│  └─────────────────────────────────────────────────────┘ │
│                        ↓                                   │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  TradingView Data API                               │ │
│  │  - 리얼타임 데이터                                  │ │
│  │  - 히스토리 데이터                                  │ │
│  │  - 기술 지표                                        │ │
│  └─────────────────────────────────────────────────────┘ │
│                        ↓                                   │
│  ┌─────────────────────────────────────────────────────┐ │
│  │  Redis 캐시                                         │ │
│  │  - 자주 요청되는 데이터 캐싱 (TTL: 1-5분)          │ │
│  │  - 성능 개선                                        │ │
│  └─────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

### 프론트엔드 데이터 흐름

```
사용자 액션 (종목 선택, 기간 변경)
    ↓
ChartTab.tsx 상태 업데이트
    ↓
TradingViewChartService.fetchData() 호출
    ↓
/api/chart/data 엔드포인트 요청
    {
      "symbol": "AAPL",
      "interval": "1h",
      "from_date": "2024-01-01",
      "to_date": "2024-11-03"
    }
    ↓
백엔드 응답 (JSON)
    {
      "symbol": "AAPL",
      "data": [
        {
          "time": 1698844800,
          "open": 170.15,
          "high": 171.50,
          "low": 169.80,
          "close": 170.85,
          "volume": 45000000
        },
        ...
      ],
      "metadata": {
        "lastUpdated": "2024-11-03T12:00:00Z",
        "cached": true
      }
    }
    ↓
ChartTab.tsx에서 데이터 처리
    ↓
TradingView Lightweight Charts에 데이터 전달
    ↓
차트 렌더링
```

---

## API KEY 관리 전략

### 1️⃣ **백엔드에서만 API KEY 저장**

```
❌ 금지
─────────────────────────────────
프론트엔드: API KEY 포함
const API_KEY = "abc123..."
const data = await fetch(
  `https://api.tradingview.com/data?key=${API_KEY}`
)
위험: 소스 코드에 노출, 클라이언트에서 볼 수 있음


✅ 권장
─────────────────────────────────
프론트엔드: 백엔드를 통해서만 요청
const data = await fetch('/api/chart/data', {
  method: 'POST',
  body: JSON.stringify({
    symbol: 'AAPL',
    interval: '1h',
    from_date: '2024-01-01'
  })
})

백엔드: 환경변수에서 API KEY 로드
TRADINGVIEW_API_KEY = os.getenv('TRADINGVIEW_API_KEY')
TradingView API에 API KEY 사용
```

### 2️⃣ **환경변수 설정**

**.env (백엔드 루트)**
```bash
# TradingView API 설정
TRADINGVIEW_API_KEY=your_api_key_here
TRADINGVIEW_API_URL=https://api.tradingview.com/v1
TRADINGVIEW_RATE_LIMIT=100  # 분당 요청 수

# 캐시 설정
REDIS_URL=redis://localhost:6379
CACHE_TTL=300  # 5분

# 환경
ENVIRONMENT=production
DEBUG=false
```

**.env.local (개발 환경)**
```bash
TRADINGVIEW_API_KEY=test_key_xxx
TRADINGVIEW_API_URL=https://api.test.tradingview.com/v1
```

### 3️⃣ **API KEY 보안 체크리스트**

```
✅ 체크리스트
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
□ API KEY는 .env 파일에만 저장
□ .env 파일은 .gitignore에 포함
□ 소스 코드에 API KEY 하드코딩 X
□ 프론트엔드에서 API KEY 전송 X
□ 백엔드 환경변수로 로드
□ HTTPS 사용 (프로덕션)
□ API KEY 정기적 로테이션 (월 1회)
□ 액세스 로그 모니터링
□ 레이트 제한 설정 (100-1000 calls/min)
□ 민감한 정보 로깅 금지
```

---

## 구현 로드맵

### Phase 1: 설계 & 준비 (1-2주)

```
1. TradingView 계정 생성 및 API KEY 발급
   └─ https://tradingview.com/developer

2. 백엔드 API 엔드포인트 설계
   └─ GET /api/chart/data
   └─ POST /api/chart/subscribe (WebSocket용)

3. 데이터 스키마 정의
   └─ OHLCV (Open, High, Low, Close, Volume)
   └─ 타임스탠프 형식 (Unix timestamp)
   └─ 응답 형식 정의
```

### Phase 2: 백엔드 구현 (2-3주)

```
1. 백엔드 의존성 설치
   pip install python-tradingview-ta  # 또는 공식 SDK
   pip install redis  # 캐싱용
   pip install requests  # HTTP 요청

2. TradingView API 래퍼 구현
   ├─ tradingview_client.py
   ├─ chart_service.py
   ├─ cache_manager.py
   └─ rate_limiter.py

3. API 엔드포인트 구현
   ├─ GET /api/chart/data (히스토리 데이터)
   ├─ GET /api/chart/realtime (실시간 데이터)
   ├─ GET /api/indicators (기술 지표)
   └─ POST /api/chart/validate (심볼 검증)

4. 캐싱 로직 구현
   └─ Redis 키: chart:{symbol}:{interval}:{date_range}
```

### Phase 3: 프론트엔드 구현 (2주)

```
1. TradingView Lightweight Charts 라이브러리 설치
   npm install lightweight-charts

2. 서비스 레이어 생성
   └─ services/tradingViewChartService.ts

3. 차트 컴포넌트 리팩토링
   └─ components/ChartTab.tsx (TradingView 사용)

4. 상태 관리 통합
   └─ 데이터 캐싱 (클라이언트)
   └─ 에러 처리
   └─ 로딩 상태
```

### Phase 4: 테스트 & 최적화 (1-2주)

```
1. 단위 테스트
   ├─ API 응답 검증
   ├─ 데이터 가공 로직
   └─ 캐시 동작

2. 통합 테스트
   ├─ 백엔드 ↔ TradingView API
   ├─ 프론트엔드 ↔ 백엔드
   └─ 엔드투엔드 테스트

3. 성능 최적화
   ├─ 캐시 히트율 분석
   ├─ API 응답 시간 측정
   └─ 번들 크기 최적화
```

---

## 데이터 포맷 명세

### 요청 형식

```typescript
// GET /api/chart/data
interface ChartDataRequest {
  symbol: string;           // "AAPL", "005930.KS"
  interval: string;         // "1m", "5m", "1h", "1d", "1w", "1M"
  from_date?: string;       // "2024-01-01" (ISO 8601)
  to_date?: string;         // "2024-11-03"
  count?: number;           // 최근 N개 캔들 (기본: 100)
  indicators?: string[];    // ["EMA", "RSI", "MACD"]
}
```

### 응답 형식

```typescript
interface ChartDataResponse {
  symbol: string;
  interval: string;
  currency: string;         // "USD" | "KRW"
  data: {
    time: number;           // Unix timestamp (초)
    open: number;
    high: number;
    low: number;
    close: number;
    volume: number;
    indicators?: {
      ema?: number;
      rsi?: number;
      macd?: {
        macd: number;
        signal: number;
        histogram: number;
      };
    };
  }[];
  metadata: {
    totalCandles: number;
    lastUpdated: string;    // ISO 8601
    cached: boolean;
    cacheTTL: number;       // 초
    nextUpdate: string;
  };
}
```

---

## 기술 스택 선택

### 프론트엔드

```
라이브러리: lightweight-charts
설치: npm install lightweight-charts

장점:
  ✅ 가볍고 빠름 (< 50KB)
  ✅ 반응형 지원
  ✅ 모바일 친화적
  ✅ 완전 커스터마이징 가능

단점:
  ❌ 기본 지표만 제공
  ❌ 고급 기능 직접 구현 필요
```

### 백엔드 (Python 예시)

```python
# 의존성
pip install requests
pip install redis
pip install flask
pip install python-dotenv

# 메인 구조
/backend/
  ├─ api/
  │   ├─ routes/
  │   │   └─ chart_routes.py
  │   └─ models/
  │       └─ chart_models.py
  ├─ services/
  │   ├─ tradingview_service.py
  │   ├─ cache_service.py
  │   └─ rate_limiter.py
  ├─ utils/
  │   ├─ validators.py
  │   └─ constants.py
  ├─ .env
  └─ requirements.txt
```

---

## 보안 고려사항

### 1️⃣ **API KEY 관리**

```
✅ DO
  - 환경변수에 저장
  - .gitignore에 .env 포함
  - 프로덕션 서버에만 진정한 KEY 저장
  - 정기적으로 KEY 로테이션
  - 사용량 모니터링

❌ DON'T
  - 소스 코드에 포함
  - 클라이언트에 전송
  - 버전 관리 시스템에 커밋
  - 공개 포럼에 공유
```

### 2️⃣ **API 호출 보안**

```
✅ DO
  - 백엔드에서만 API 호출
  - 요청 검증 (심볼, 기간 등)
  - HTTPS 사용
  - 액세스 토큰 검증
  - 로깅 및 모니터링

❌ DON'T
  - 클라이언트에서 직접 API 호출
  - 입력값 검증 없이 처리
  - HTTP 사용 (프로덕션)
  - 민감한 정보 로깅
```

### 3️⃣ **레이트 제한**

```python
# 백엔드에서 구현
from functools import wraps
import time

class RateLimiter:
    def __init__(self, calls_per_minute=100):
        self.calls_per_minute = calls_per_minute
        self.calls = []

    def __call__(self, f):
        @wraps(f)
        def wrapper(*args, **kwargs):
            now = time.time()
            # 1분 이내의 호출만 유지
            self.calls = [c for c in self.calls if now - c < 60]

            if len(self.calls) >= self.calls_per_minute:
                raise Exception("Rate limit exceeded")

            self.calls.append(now)
            return f(*args, **kwargs)
        return wrapper

# 사용
@app.route('/api/chart/data')
@RateLimiter(calls_per_minute=100)
def get_chart_data():
    pass
```

---

## 마이그레이션 체크리스트

```
프로덕션 배포 전 확인 사항
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

데이터
  □ 백엔드에서 TradingView API 호출 성공
  □ 데이터 형식 검증 (OHLCV)
  □ 모든 심볼에 대해 테스트
  □ 캐싱 동작 확인

API
  □ 모든 엔드포인트 문서화
  □ 에러 처리 구현
  □ 레이트 제한 설정
  □ 액세스 로그 활성화

프론트엔드
  □ Lightweight Charts 렌더링 확인
  □ 모든 기간(1m~1y) 테스트
  □ 반응형 디자인 확인
  □ 모바일 호환성 테스트

보안
  □ API KEY 환경변수 설정
  □ .env 파일 .gitignore에 추가
  □ HTTPS 활성화
  □ 입력값 검증 구현
  □ CORS 정책 설정

성능
  □ 캐시 히트율 > 70%
  □ API 응답 시간 < 500ms
  □ 번들 크기 측정 및 최적화
  □ 로드 테스트 완료

모니터링
  □ 에러 로깅 시스템
  □ 성능 메트릭 수집
  □ 알림 설정 (API 실패, 응답 시간 초과)
```

---

## 참고 자료

```
공식 문서
  - https://tradingview.com/developer/
  - https://github.com/TradingView/lightweight-charts

대체 솔루션
  - Alpha Vantage: https://www.alphavantage.co/
  - IEX Cloud: https://iexcloud.io/
  - Finnhub: https://finnhub.io/

라이브러리
  - lightweight-charts: https://github.com/TradingView/lightweight-charts
  - python-tradingview-ta: https://github.com/bybit-exchange/
```

